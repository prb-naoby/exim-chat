version: '3.8'

services:
  exim-chat:
    build: .
    container_name: exim-chat
    ports:
      - "8504:8504"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
